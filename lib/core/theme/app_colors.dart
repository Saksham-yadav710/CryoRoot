import 'package:flutter/material.dart';

class AppColors {
  // Brand & Agriculture Identity
  static const Color primary = Color(0xFF059669); // Emerald Forest
  static const Color primaryDark = Color(0xFF064E3B);
  static const Color primaryLight = Color(0xFFE6F7F0);
  static const Color primaryContainer = Color(0xFFD1FAE5);

  static const Color secondary = Color(0xFF0284C7); // Clean Cool Blue (Cooling)
  static const Color secondaryContainer = Color(0xFFE0F2FE);

  static const Color solarGold = Color(0xFFD97706); // Warm Amber/Gold (Solar)
  static const Color solarContainer = Color(0xFFFEF3C7);

  static const Color pcmCyan = Color(0xFF0891B2); // PCM Cold Reserve
  static const Color pcmContainer = Color(0xFFCFFAFE);

  // Status System (Strictly adhere to requirement)
  // GREEN: GOOD / SAFE
  static const Color statusGood = Color(0xFF16A34A);
  static const Color statusGoodBg = Color(0xFFDCFCE7);
  static const Color statusGoodBorder = Color(0xFF86EFAC);

  // YELLOW: ATTENTION
  static const Color statusAttention = Color(0xFFCA8A04);
  static const Color statusAttentionBg = Color(0xFFFEF9C3);
  static const Color statusAttentionBorder = Color(0xFFFDE047);

  // ORANGE: WARNING
  static const Color statusWarning = Color(0xFFEA580C);
  static const Color statusWarningBg = Color(0xFFFFEDD5);
  static const Color statusWarningBorder = Color(0xFFFDBA74);

  // RED: CRITICAL
  static const Color statusCritical = Color(0xFFDC2626);
  static const Color statusCriticalBg = Color(0xFFFEE2E2);
  static const Color statusCriticalBorder = Color(0xFFFCA5A5);

  // GREY: INACTIVE / OFFLINE
  static const Color statusOffline = Color(0xFF64748B);
  static const Color statusOfflineBg = Color(0xFFF1F5F9);
  static const Color statusOfflineBorder = Color(0xFFCBD5E1);

  // Surfaces & Backgrounds
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceElevated = Color(0xFFFFFFFF);
  static const Color border = Color(0xFFE2E8F0);
  static const Color borderSubtle = Color(0xFFF1F5F9);

  // Typography Colors
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF475569);
  static const Color textTertiary = Color(0xFF94A3B8);
  static const Color textInverse = Color(0xFFFFFFFF);
}
